---
layout: "@/layouts/global.astro"
title: "OpenAI's WebRTC Problem: Part 2"
author: kixelated
description: Stuff was added, stuff was removed, stuff was changed for no reason at all.
cover: "/blog/update-00/globe.png"
date: 2026-07-31
---

# OpenAI's WebRTC Problem: Part 2
On a previous episode, I [flammed OpenAI for using WebRTC](/blog/webrtc-is-the-problem) and it went [viral on Hacker News](https://news.ycombinator.com/item?id=48051951).

neat

A huge shoutout to Sean DuBois and Justin Uberti (WebRTC **experts** at OpenAI) for reaching out after my unhinged rant.
You could have just said `@kixelated ur dumb` but you didn't.

Anyway, my divinely anointed crusade against WebRTC is not over.
I yearn for a **follow-up**.

## Hot Take Alert
Let's say you're a professional prompter working on a voice AI product.\
Let's say you're making an obscene amount of cash.\
Let's say one of your users verbalizes: `what is le capital of France?`

If your AI ever replies with:
> I'm sorry, you broke up, can you repeat that?

**You fucked up**. 
You picked WebRTC for voice AI.

## Enter the Antagonist
Then why is [OpenAI using WebRTC](https://openai.com/index/delivering-low-latency-voice-ai-at-scale/)?

WebRTC is designed for conference calls, aka real humans use real voices.
It's available in every browser via [libwebrtc](https://webrtc.googlesource.com/src), a C++ monolith maintained by Google.
It's basically the **Google Meet client** available to everyone via a [horrific API](https://webrtchacks.com/sdp-anatomy/).
Yes, it's built on VoIP standards [from the 90s](https://datatracker.ietf.org/doc/html/rfc2327).

The standard is **so complicated** that over a decade later, Firefox/Safari still use Chrome's implementation verbatim.
The best (and most complex) parts of WebRTC aren't even standardized: NetEQ, AEC, GCC, etc.
It doesn't matter what the 30+ RFCs say, **the standard is what Google has decided to implement**.

Google tailors WebRTC to whatever their product needs.
Meanwhile, literally every other conferencing app encourages a native client rather than be **forced to use WebRTC** in a browser.

quite the anti-competitve ruse

How do they get away with it?
Frankly, Google has done an amazing job.
WebRTC is the state of the art when it comes to real-time conversational audio, and it's not even close.

It seems like a no-brainer for voice AI.  ...right?

The problem is that Google Meet, and thus `libwebrtc`, and thus WebRTC, is **hard-wired for human latency**.

## Clocks and Rates
We flesh bags consume/produce audio in "real-time", aka a **human clock rate**.
Our neurons can only fire so fast.
Native speakers might be able to handle Youtube at 2x speed, but eventually it turns into gibberish.

That's why WebRTC doesn't bother sending an audio packet twice.
Our brains need a constant stream of audio to process, not content from 5 seconds ago at 20x the speed.

But **wake up old man**, it's not 2020 any longer.
The internet is no longer for our flabby lips.

Our robot overlords produce/consume audio based on a **silicon clock rate**.
We shoved electricity into a rock and now it can read a library of audio books in minutes.
What a time to be alive.

Voice agents can **process and generate** audio faster than real-time.
The agent sits there patiently waiting for us to finish our prompt (STT).
It thunks (LLM).
Then it mimics our vocal cords to create a voice response (TTS).
But the response has to be played back slowwwwly to not overwhelm our itty bitty brain.

Alas, WebRTC **cannot go faster than real-time**.
A packet is sent exactly once at the **exact** moment it is meant to be heard.
And if a packet is lost on the network, **that sucks**, better luck next RTT.

## I Require Context
It gets worse, because a prompt is not the same as a conversation.

Let's say you utter: `What is le capital of France?`.
It takes 4 seconds for you to utter *those fateful words*.
You expect a correct response from either the AI or human on the other end.

Humans and AI models need enough context to answer the prompt.
Not *everything* needs to be there; just enough to read between the lines.

`What ... capital ... France?` is good enough.\
`.... ... capital ... France?` is might be okay? \
`What ... ....... ... France?` is not enough context.

caveman speak, ai hear.\
words missing, result be bad.\
no want bad.

## I Drop Context
Let's say there's a 1 second blip of network congestion at the start.
Every packet during that period is lost forever within the dark abyss.

WebRTC doesn't attempt to retransmit, so the word `What` is lost.
It's reasonable for humans; our brains prefer reconstructing context rather than a delay+burst.

However, we're at the start of the sentence, so there are still **4 seconds** until the word `Paris` is uttered.
Voice AI still has **4 seconds** to retransmit the context **and still be on time**.

**4 seconds** is a lifetime in computing.
We literally have **4 seconds** to retransmit **6 UDP packets** (1 second @ 64Kb/s).

And if we manage to accomplish that *herculean feat*, our voice AI can process it faster than real-time.
The user will notice zero impact to the latency or quality of the AI response.

## A Better Transport
Anyway, that's your hyper deluxe OpenAI voice AI model, after billions invested, has the audacity to ask: 
**can you repeat that?**

I could dump on WebRTC for days, but I want to be positive for a change.
Go read the previous [blog post](/blog/webrtc-is-the-problem) if you want more slander.

In my humble and handsome opinion, **it's time to invest in a better transport**.
Advanced models can try to fill in the gaps caused by packet loss, *but they shouldn't have to*.

A voice agent should **never** ask the **user** to retransmit a prompt.\
A voice agent should **instead** ask the **transport** to retransmit a prompt.


## Enter The Protagonist
Let's take a quick break from theory-crafting.

[Pipecat now supports MoQ](https://github.com/pipecat-ai/pipecat/pull/4629)!
A huge thank you to [Vanessa](https://github.com/vipyne) for putting in the work and putting up with my constant "improvements".

It's still very rough around the edges.
It's been 3 months since the last Voice AI blog post... and I've only spent like 3 hours of ~work~ prompting since then.
You gotta acquihire [Kixel, Corp.](https://kixel.us) if you want me to focus on Voice AI.

Here's a crude demo with my credit card deets attached. 
please don't milk me dry.

DEMO

Back to our regularly scheduled mono-logging.
I need to explain *how* MoQ is better than bread.

## Latency Budgets

Every subscription includes a **maximum latency** before a frame is skipped.
The intent is to support multiple different latency budgets:

- A pilot might want 0.1s of latency
- A spectator might want 2s of latency
- An archive worker might want 30s of latency
- etc

But it works for voice AI too!
The key is that **maximum** word, because the minimum latency is always 0.
We transmit new frames ASAP while old frames might take up to the **maximum latency**.

But it gets better.

## Text-to-Speech

## Latency Budgets
A strawman might ask: "isn't lower latency always better?"

no mr strawman, it's not.

**There's a fundamental trade-off between latency and quality**.

The more time you have to work on something, the better the result.
Good things take time, like Fable after a reset.
That happened to line up with a competitor's launch.

Similar to LLMs, networks are lossy, non-deterministic black-boxes.
The more time you have to transmit something, the better the reliability.

The amount of time a user is willing to wait for something is a **product decision**.
(gotta give the PMs something to do).
Do users want something fast and lossy, or slow and reliable?

WebRTC has no such knob to control the latency budget\*.
The decision that users want fast and lossy is hard-coded into the protocol.

\* Yes WebRTC nerds there is an extension **but it's not supported for audio**.
We'll explain how to fix WebRTC later.

The key MoQ feature is that every subscription includes a **maximum latency** before a frame is skipped.

Humans generally want 

> Codex: One second, let me look that up.

As far as I understand, the approach a cheap/fast speech-to-speech model to generate *comfort noise*.

> Human: Hey Codex, what is le capital of France?
> Codex: One second, let me look that up.

Boom, that useless "I'm thinking" response makes the human feel good chemicals.
More importantly, it bought us a few more seconds for our state-of-the-art model to generate a proper response.
Possibly even via the "old" approach SST -> LLM -> TTS pipeline.

Speech-to-Speech doesn't invalidate latency budgets.
It is not a silver bullet that somehow redeems WebRTC's over-aggressive nature.

**Your network transport should be aligned with your model's latency budget.**



## Latency Budgets
A strawman might ask: "isn't lower latency always better?"

no mr strawman, it's not.

**There's a fundamental trade-off between latency and quality**.

The more time you have to work on something, the better the result.
Good things take time, like Fable after a reset.
That happened to line up with a competitor's launch.

Similar to LLMs, networks are lossy, non-deterministic black-boxes.
The more time you have to transmit something, the better the reliability.

The end goal is to have **your network transport align with your AI model.**

- If you STT model requires  
- If your TTS model generates 30s of audio in advance, your network transport should transmit 30s of audio in advance.

## I Require Context
Let's say you're running an old-school STT like Whisper that needs an entire


Audio packets are *tiny*, 8K bytes per second.
We can easily store them in RAM and retransmit them when the network recovers.
They yearn to be useful.

**Fun Fact**: I yearn to use the word "yearn".


## I Require Context
The problem arises when your AI model and network **disagree on the latency budget**.

Suppose you ask the question: `What is le capital of France?`

WebRTC has a hyper-aggressive latency budget. 
It will drop arbitrary audio packets to keep latency real-time.

But voice AI agent require context.
`What is le capital ...`, with the last word missing, is not enough context.
It can fill the blanks much of the time, but every ambiguity hurts the accuracy.


## Speech-to-Speech Fixes Everything
The hot new broad in voice AI is [speech-to-speech](https://openai.com/index/introducing-the-realtime-api/).

The idea is to cut out the text middleman.
STT -> LLM -> TTS gets combined into one pipeline, reducing latency while preserving verbal context.
OpenAI is the pioneer in this space and they deserve **mad props**.

So now the latency of voice AI is lower than ever and WebRTC is vindicated!

**Obviously not**, we're not even half way through this post bub.
The **fundamental** trade-off between latency and quality still applies.

As far as I can tell, ChatGPT uses Speech-to-Speech to generate **comfort noise**.

> Human: What is le capital of France?
> Codex: One second, let me look that up.

Boom, that useless "I'm thinking" response makes the human feel good chemicals while buying more time for the expensive model to generate a proper response.

Speech-to-Speech doesn't invalidate latency budgets.
It is not a silver bullet that somehow redeems WebRTC's over-aggressive nature.


## Enter The Protagonist
Oh hey, I created this thing called [Media over QUIC](https://moq.dev).\
Also known as `em oh queue` or `mock`.
However you want to prononce it.

It's that better transport btw.

[Pipecat now supports MoQ](https://github.com/pipecat-ai/pipecat/pull/4629)!
A huge thank you to [Vanessa](https://github.com/vipyne) for putting in the work and putting up with my constant "improvements".

Why is this a big deal?

Every MoQ subscription indcates a **maximum delay** before a frame is too old and skipped.
Users get a different, and adjustable experience, based on the **latency they’re willing to tolerate**.


> Codex: One second, let me look that up.

As far as I understand, the approach a cheap/fast speech-to-speech model to generate *comfort noise*.

> Human: Hey Codex, what is le capital of France?
> Codex: One second, let me look that up.

Boom, that useless "I'm thinking" response makes the human feel good chemicals.
More importantly, it bought us a few more seconds for our state-of-the-art model to generate a proper response.
Possibly even via the "old" approach SST -> LLM -> TTS pipeline.

Speech-to-Speech doesn't invalidate latency budgets.
It is not a silver bullet that somehow redeems WebRTC's over-aggressive nature.

**Your network transport should be aligned with your model's latency budget.**


## Prove It
Enough theory, [Pipecat now supports MoQ](https://github.com/pipecat-ai/pipecat/pull/4629)!
A huge thank you to [Vanessa](https://github.com/vipyne) for putting in the work and putting up with my constant "improvements".

It's still very rough around the edges.
It's been 3 months since the last Voice AI blog post... and I've only spent like 3 hours of ~work~ prompting since then.
You gotta acquihire [Kixel, Corp.](https://kixel.us) if you want me to focus on Voice AI.

Here's a crude demo with my credit card deets attached. 
please don't milk me dry.

DEMO

I'll explain how it works but first, we gotta cover something very important.
Back to our regularly scheduled mono-logging.

## Speech-to-Speech
So it turns out the latest development in voice AI is [speech-to-speech](https://openai.com/index/introducing-the-realtime-api/).
Oops, I probably should have done *some* research on the state of voice AI before writing that last post.

**DISCLAIMER**: I barely researched the state of voice AI before writing this blog post.
I'm talking out of my ass (again).
I give you full permission to tweet `@kixelated ur stupid`.

A quick summary:
- **Old**: `Human -> VAD -> STT -> LLM -> TTS -> Human`
- **New**: `Human -> STS -> Human`

Some definitions because acronyms sure make things easier to understand:
- **VAD**: Voice Activity Detection
- **STT**: Speech-to-Text
- **TTS**: Text-to-Speech
- **STS**: Speech-to-Speech
- **LLM**: You know what it is
- **Human**: Pays the electricity bill

Anyway, the *new pipeline* is to have the robot brain input/output audio.
It's super cool, reduces latency while preserving verbal context, and I must give OpenAI *huge* props for this innovation.

So if the model supports real-time latency, that means WebRTC is back on the table... right?
**Not so fast.**



## MoQ is Amazing
Anyway, here's the nerdy details about the MoQ support in Pipecat.
Assuming a traditional `VAD -> STT -> LLM -> TTS` pipeline.

All of these constants can be adjusted btw.
I just thought it would make the blog post more readable.

**STT is (re)transmitted for up to 5 seconds.**
The agent has a jitter buffer (of the same size) which flushes in order, immediately when there's no gaps.
- Best case: 0s latency
- Worst case: 5s latency (temporary)

**STT/LLM can process faster than real-time, absorbing some of this latency.** 
The latency at the end of a sentence/thought is the main thing that matters.
- Best case: "Hey could you..." is late.
- Worst cast: "capital of France" is late.

**TTS is (re)transmitted for up to 200ms.**
The player has a jitter buffer (of the same size) which flushes in order, skipping gaps eventually.
- Best case: 200ms latency
- Worst case: 200ms latency, some audio is dropped

**TTS is buffered for up to 30s.**
The player uses timestamps to know when to emit frames.
Anything in the future is buffered (like a Youtube video), so audio is not dropped during spikes of congestion.
- Best case: 30s in RAM
- Worst case: 0s in RAM, some audio is dropped

**Interruptions will reset audio.**
If Opus 5 is being too verbose, ask something else and we'll drop the previous TTS response.
This cancels any transmissions and any buffered audio.
- Worst case: Some wasted tokens.

I'll spare you the full WebSocket vs WebRTC vs MoQ comparison because I'm already rambling.

Spoiler: **MoQ is a significant improvement for voice AI**.

## Context is Delta-Encoded
TODO Dropping "What is" is useless.
We need that context to understand the user's request.


## More Free Ideas
Hey, Anthropic.
I'm sorry that I called you the orange butthole company.
We may have gotten off on the wrong foot.

Hey, OpenAI.
I'm sorry for this blog post.
We may have gotten off on the wrong foot.

I'll make it up to both of you.
Here's some free ideas you could prototype with unlimited Fable/Astro access.
Just please tell me if any of them are worth it.

### Aggressive Speech-to-Speech, Conservative STT->LLM->TTS
Placeholder constants btw.

- Set `max-publisher-latency` to 1s for STS.
- Set `max-publisher-latency` to 5s for STT.

Under normal conditions, the speech-to-speech pipeline will be zero latency.
During congestion, latency will increase a little bit until we get some loss/distortion, but it's still kept snappy enough.
Maybe the model can still figure out the end of a sentence and "let me look that up for you" or whatever.

Meanwhile, the STT->LLM->TTS pipeline will wait for *up to* 5s for audio gaps to be filled before starting work.
The idea is that you don't waste tokens on distorted/wrong prompts during congestion.
Accuracy is more important than latency for the good reply.

**Fun fact**, MoQ supports this for free.
Two subscribers with different latencies can share a single upstream subscription.
It's all about the size of the jitter buffer baybee.

### Decouple Compute
- Run `moq-relay` within CDN edges.
- Run models where compute is cheap.

Maybe this idea is already outdated given the obscene amount of capital being poured into datacenters, but hear me out.

CDNs run edge nodes as close to users as possible.
Your ISP is (statistically) shit, so the whole business model of CDNs is to minimize the lossy first/last mile.
Once on the CDN's network (ex. Cloudflare) they can over-provision and deduplicate traffic to improve the quality.

But like, there's only a finite number of graphics cards we can efficiently put in those edges.
So the idea is to run voice AI agents independently of the CDN.

**Pipecat already supports this!**
- Agents and users connect to a MoQ CDN, both as a client using the shortest path.
- A user announces `request/<id>` to the CDN.
- An agent discovers `request/<id>`, grabs a lock, subscribes to it, and announces `response/<id>`.
- The user discovers `response/<id>` and subscribes to it.

Why is this convoluted reinvention of HTTP cool?
**You can independently scale network and compute.**

Unlike HTTP, MoQ origins are dynamic.
A client connects to the CDN and announces "I am the publisher of `foo/bar`".
The CDN forwards that announcement to any authorized subscribers and now knows how to route subscriptions.

**END RESULT**

Run agents where compute is cheap.
Only announce when an agent has capacity.

Run the CDN edge nodes close to the user.
Route incoming connections based on available capacity (ex. anycast/GeoDNS).
[Or pay me to do it](https://moq.pro)

### Shared Infrastructure
**Question**: What does the `M` in `MoQ` stand for?
**Answer**: It stands for `mostly Media but actually anything`.

At the end of the day, MoQ is a pub/sub protocol.
We're trying to make a live version of HTTP.
The payload be opaque to the CDN.

It makes for a super reusable protocol.

## How to Fix WebRTC
Wow this blog post never ends huh.
**It's possible to fix WebRTC!**

For example, WebRTC supports [Playout Delay](https://webrtc.googlesource.com/src/+/main/docs/native-code/rtp-hdrext/playout-delay/README.md) via an SDP/RTP extension.
On paper this is perfect; you now get a knob to control the maximum/minimum latency just like MoQ!

Unfortunately, this experimental extension from [2019](https://bugzilla.mozilla.org/show_bug.cgi?id=1585009) only works for video in `libwebrtc`.
Apparently.
I've never actually tried it.

There's a lot of things like that.
For example, RTX is not supported for audio withing `libwebrtc`.
The problem is rarely the wire transport.

The problem with WebRTC is are the stewards.
The Google engineers behind WebRTC are brilliant... but the company needs a use-case to justify every feature.
They are effectively maintaining the Google Meet client and making it available as a browser API.

## Just Fork It Lul




## Conclusion
